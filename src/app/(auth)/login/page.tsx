import Login from "@/components/Auth/Login/Login";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default Page;
